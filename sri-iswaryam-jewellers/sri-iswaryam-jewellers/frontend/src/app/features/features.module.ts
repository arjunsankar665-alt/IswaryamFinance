import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

// This module only imports other feature modules
// Each feature has its own module with declarations

@NgModule({
  declarations: [],
  imports: [
    CommonModule
  ]
})
export class FeaturesModule { }
